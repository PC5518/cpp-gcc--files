#include <iostream>
int main() {
    int x =23;
    //int -> declares the type and 23 assign the value
    // let's see what if suffix is there
    // eg.  100u this tells it's unsigned or non negative value storer. 
    int x =100u;
    // this is equal to 
    // now but the thoughtprocess behind the compiler is different 
    unsigned int x=  100;


}