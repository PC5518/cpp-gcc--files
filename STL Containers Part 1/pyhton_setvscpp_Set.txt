internally python is totally different from cpp's set. python uses hash table behind for set.
cpp uses balanced tree. 
the only thing is that python and cpp both removes the duplicated elements as per the set definition and c++ in addition to removing the duplicate it also makes it sorted.